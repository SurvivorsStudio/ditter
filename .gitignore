# ── 시크릿 (절대 커밋 금지) ──────────────────────────────────────────────
.env
.env.*
!.env.example

# ── Python ───────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ── Node / 프론트엔드 ────────────────────────────────────────────────────
node_modules/
apps/web/dist/
dist/
*.tsbuildinfo

# ── SymmetricDS 엔진 설정 (DB 비밀번호가 평문으로 들어간다) ─────────────
# 예시(.example)만 커밋한다. 채운 파일은 절대 올리지 않는다.
/sync/symmetricds/engines/*.properties
/sync/symmetricds/conf/*.properties
/sync/symmetricds/lib/*.jar

# ── CDC 임시 테스트 세트 (배포 반영 금지) ────────────────────────────────
# 로컬에서 CDC 를 시험하려고 만든 임시 소스/타깃 구성. 실제 배포와 무관.
/docker-compose.cdc-test.yml
/docker-compose.mssql-cdc-test.yml
/docker-compose.pg-cdc-test.yml
/cdc-test/

# ── 로컬 도구 설정 (개인 환경) ───────────────────────────────────────────
# .claude 의 워크플로(commands·agents·hooks·settings.json)는 팀 공유 툴링이라 추적한다.
# 사람마다 다른 로컬 파일만 무시한다.
/.claude/settings.local.json
/.claude/.review-state.json

# ── OS / 에디터 ──────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
