# 보안 취약점 신고

ditter 는 운영 중인 데이터베이스에 붙는 도구다. 읽기 전용 경계·자격증명 보관·인증/권한에서
생긴 문제는 **공개되는 순간 그대로 악용될 수 있다.** 그래서 취약점은 이슈·PR·토론 등
**공개된 자리에 적지 않는다.**

## 어디로 신고하나

**[비공개 취약점 신고 열기](https://github.com/SurvivorsStudio/ditter/security/advisories/new)**

GitHub 의 비공개 취약점 신고(Private vulnerability reporting)를 쓴다. 신고 내용은 메인테이너와
신고자에게만 보이고, 수정이 끝난 뒤에 함께 공개할지 정한다. GitHub 계정만 있으면 되고
따로 메일 주소를 알 필요가 없다.

## 언제 답이 오나

- **영업일 5일 이내**에 접수 확인을 보낸다. 그 안에 답이 없으면 놓친 것이니
  [SurvivorsStudio](https://github.com/SurvivorsStudio) 메인테이너에게 한 번 더 알려 달라.
- 공개 시점은 **수정을 배포한 뒤**, 늦어도 **신고로부터 90일** 안에 신고자와 합의해 정한다.
  합의 없이 먼저 공개하지 말아 달라 — 이 도구는 운영 DB 에 붙는다.

## 무엇을 적나

- 영향을 받는 영역 (`apps/api` · `apps/web` · `apps/worker` · `apps/connectors` · `apps/sap-connector`)
- 재현 절차와 확인한 커밋 해시 (`git rev-parse --short HEAD`)
- 실제로 일어나는 일 — 무엇을 읽거나 쓸 수 있게 되는지
- 로그·화면을 붙일 때는 **접속 정보와 실제 데이터를 가린 뒤** 붙인다

## 특히 알고 싶은 것

- **읽기 전용 경계 우회** — `ensure_statement_allowed` · `ensure_select_only` 를 지나
  허용하지 않은 문장이 실행되는 경로 (이 저장소에서 되돌리기 가장 어려운 경계다)
- **자격증명 노출** — 연결 설정의 비밀값이 API 응답·로그·오류 메시지·생성 코드에 드러나는 경우
- **인증/권한 우회** — 로그인·JWT·역할(RBAC) 검사를 건너뛰는 경로
- **SQL 주입** — 변수 치환(`$변수` · `${노드.컬럼}`)이나 연합 조회 재작성을 통해 주입이 성립하는 경우

## 범위 밖

- `demo/` 시연 스택의 기본 비밀번호. 값이 `demo/.env.example` 에 그대로 커밋돼 있는 것은
  의도된 것이고, 그 포트는 `127.0.0.1` 에만 열린다.
- 인증을 끈 로컬 개발 설정(`EAI_AUTH_ENABLED=false`)에서만 성립하는 문제.
- 운영자가 직접 쓰기를 허용한 연결에서 쓰기가 되는 것 (그것이 그 설정의 목적이다).

## 지원 범위

배포된 릴리스가 아직 없다. `main` 의 최신 커밋만 수정 대상이다.
