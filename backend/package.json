{
  "name": "@ditter/backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch --watch-path=src --watch-path=migrations --env-file-if-exists=../.env src/index.ts",
    "dev:poll": "node scripts/dev-watch.mjs",
    "start": "node --env-file-if-exists=../.env src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@ditter/shared-types": "*",
    "fastify": "^5.6.1"
  }
}
